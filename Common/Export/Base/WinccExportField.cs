namespace Dmc.Siemens.Common.Export.Base
{
    public enum WinccExportField
    {
        // Tags
        Name,
        Connection,
        PlcTag,
        DataType,
        Length,
        Address,

        // Alarms
        Id,
        AlarmText,
        TriggerTag,
        TriggerBit,
        InfoText
    }
}
